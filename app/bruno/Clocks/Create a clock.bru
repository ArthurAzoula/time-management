meta {
  name: Create a clock
  type: http
  seq: 2
}

post {
  url: {{BASE}}/api/clocks/1
  body: none
  auth: none
}

tests {
  test("Create a clock", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(201);
  });
}
